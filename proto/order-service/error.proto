syntax = "proto3";
package order_service;

enum OrderServiceErrorType {
  UNDEFINED = 0;
  SERVICE_UNAVAILABLE = 1;
  ORDER_NOT_FOUND = 2;
}

message OrderServiceError {
  OrderServiceErrorType type = 1;
  string message = 2;
}