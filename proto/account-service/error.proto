syntax = "proto3";
package account_service;

enum AccountServiceErrorType {
  UNDEFINED = 0;
  SERVICE_UNAVAILABLE = 1;
  ACCOUNT_ALREADY_EXISTS = 2;
  ACCOUNT_DOESNT_EXIST = 3;
}

message AccountServiceError {
  AccountServiceErrorType type = 1;
  string message = 2;
}
